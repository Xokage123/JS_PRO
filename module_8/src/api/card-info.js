import CardInfo from 'card-info';

export const generateInfoCard = (number) => {
  const cardInfo = new CardInfo(number);
  // console.log('Название банка:', cardInfo.bankName);
  // console.log('Логотип банка:', cardInfo.bankLogo);
}